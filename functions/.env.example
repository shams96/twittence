ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Optional: enables the Google Lighthouse SEO score comparison shown alongside our own SEO score.
# Get a free key: console.cloud.google.com > APIs & Services > Credentials > Create API Key,
# then enable the "PageSpeed Insights API" for that project. Leave unset to skip the comparison.
# PAGESPEED_API_KEY=

STRIPE_PRICE_FOUNDING=
STRIPE_PRICE_STANDARD=
FOUNDING_CAP=100

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
